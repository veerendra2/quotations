---
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin

archives:
  - formats: ["zip"]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

brews:
  - name: funmotd
    url_template: "https://github.com/veerendra2/funmotd/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    directory: Formula
    caveats: >-
      ℹ️ To display a random quote when the terminal opens, add it to your shell
      💡 Example for Bash:
      echo "funmotd" >> ~/.bashrc
    homepage: "https://github.com/veerendra2/funmotd"
    description: "TV Shows and Movies Quotes MOTD for Shell 😎"
    license: "MIT"
    repository:
      owner: veerendra2
      name: homebrew-tap
      branch: main
      token: "{{ .Env.GITHUB_TOKEN }}"

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
